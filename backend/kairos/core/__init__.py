# Reserved for shared constants, exceptions, and DB helpers (RFC v1.0 §4.1).
# Populated starting Phase 4, when the service layer first needs them —
# empty until then rather than pre-built against a shape that doesn't exist yet.
